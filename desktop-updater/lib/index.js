export const inject = [];

export function apply() {
  // The desktop updater is a browser-only feature. This host entry lets the
  // Harness loader discover the package and serve its dsh.client module.
}
